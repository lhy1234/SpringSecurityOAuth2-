server:
  port: 8004



security:
  oauth2:
    resource:
      jwt:
        key-uri:  http://localhost:8002/oauth/token_key  #获取解析jwt，验签名key的路径
      user-info-uri:
    client:
      client-id:  farinfoTraining #获取验签key需要身份认证，这里是网关的clientId
      client-secret:  123456  #获取验签key需要身份认证，这里是网关的secret